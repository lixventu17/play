Qual è la sintassi corretta per stampare un messaggio in Java?
System.println("Ciao");
System.out("Ciao");
System.out.println("Ciao");
System.print("Ciao");
2

Qual è il metodo corretto per ottenere la lunghezza di una stringa in Java?
string.length;
string.len();
string.size();
string.length();
3

Quale parola chiave è utilizzata per creare una nuova istanza di una classe in Java?
instance
new
create
init
1

Come si dichiara una variabile intera in Java?
int x;
integer x;
num x;
x int;
0

Qual è l'operatore di uguaglianza in Java?
=
==
===
!=
1

Quale parola chiave viene utilizzata per ereditare una classe in Java?
inherits
extends
implements
overrides
1

Qual è il costrutto corretto per un ciclo for in Java?
for (int i; i < 10; i++)
for (int i = 0; i < 10; i++)
for i in range(10)
for (int i = 0, i < 10, i++)
1

Qual è la classe principale per le interfacce grafiche in Java?
Graphics
UI
Swing
JFrame
3

Come si chiama il metodo che viene chiamato quando si crea un'istanza di una classe in Java?
class()
init()
new()
constructor()
3

Qual è il tipo di dato utilizzato per rappresentare una sequenza di caratteri in Java?
String
Character
char[]
string
0